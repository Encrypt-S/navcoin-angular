export class WalletOverview {
  currentBlock: number;
  highestKnownBlock: number;
  isLocked: Boolean;
  isStaking: Boolean;
  isEncrypted: Boolean;
  isSyncing: Boolean;
  walletChain: number;
  isUnlockedForStaking: Boolean;
  walletVersion: number;
}
